name = input("Please enter your name: ")
print(name)

# Showing some built in string functions
whitespace = "   There is some white space   "
rightstripped = whitespace.rstrip()
leftstripped = whitespace.lstrip()
stripped = whitespace.strip()

print(rightstripped)
print(leftstripped)
print(stripped)
